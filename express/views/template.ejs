<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>template</title>
    </head>
    <body>
        <h1><%= name %></h1>
        <h2>Proyectos</h2>
        <ul>
            <% projects.forEach(function(project) { %>
                <li>
                    <%= project.name %> - <%= project.description %>
                </li>
            <% }); %>
        </ul>
    </body>
</html>
